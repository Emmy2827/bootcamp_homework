function calcCircumference(radius)
{
    var calcCircumference = 3.14 * 2 * radius;
    console.log(" The Circumference is " + calcCircumference);
}
calcCircumference(5)
function calcArea(radius)
{
    var calcArea = 3.14 * radius * radius;
    console.log(" The area is " + calcArea);
}

calcArea(31.41)

